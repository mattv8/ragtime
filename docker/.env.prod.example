# =============================================================================
# Ragtime RAG API - Production Environment
# =============================================================================
# Copy to .env.prod and configure with production values

# -----------------------------------------------------------------------------
# Database Configuration (Prisma)
# -----------------------------------------------------------------------------
# PostgreSQL connection URL for Prisma ORM (ragtime internal database)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://ragtime:your-secure-password@ragtime-db:5432/ragtime

# -----------------------------------------------------------------------------
# Security Configuration (MUST CONFIGURE FOR PRODUCTION)
# -----------------------------------------------------------------------------
# API Key - REQUIRED for production
API_KEY=your-secure-api-key-here

# CORS - specify your domains
ALLOWED_ORIGINS=https://your-domain.com,https://app.your-domain.com

# -----------------------------------------------------------------------------
# Authentication Configuration (MUST CONFIGURE FOR PRODUCTION)
# -----------------------------------------------------------------------------
# JWT secret key - REQUIRED for production (generate with: openssl rand -base64 32)
JWT_SECRET_KEY=your-secure-jwt-secret-here

# Session cookie - MUST be true for HTTPS
SESSION_COOKIE_SECURE=true

# Local admin fallback account
# Username will be prefixed with "local:" in database to avoid LDAP collision
LOCAL_ADMIN_USER=admin
LOCAL_ADMIN_PASSWORD=your-secure-admin-password

# Note: LDAP configuration is stored in the database and managed via the Settings UI
