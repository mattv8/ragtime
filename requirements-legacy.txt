# =============================================================================
# Ragtime - Legacy CPU Dependencies
# =============================================================================
# This file contains pinned versions for older CPUs without X86_V2 support.
# NumPy 2.x requires X86_V2 instructions; rawpy 0.22+ requires NumPy 2.x.
# These older versions are built for broader CPU compatibility.
#
# Use the main requirements.txt and install these overrides for legacy builds.
# =============================================================================

# NumPy 1.x - compatible with older CPUs without X86_V2 support
numpy>=1.24.0,<2.0.0

# rawpy 0.21.x - last version compatible with NumPy 1.x
rawpy>=0.19.0,<0.22.0
